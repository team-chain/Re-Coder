# 야간 배치 결과 (2026-09-13)

기준: develop `d7b2557` (PR #40까지 머지된 상태)

## 만든 브랜치 5개

| 브랜치 | 내용 | 노션 카드 |
|---|---|---|
| `feat/workbench-revive` | Workbench 사이드바 버튼 부활 | P0 · 리뷰로 이동 |
| `fix/placeholder-responses` | 실패가 200 OK로 나가던 응답 제거 | P1 · 리뷰로 이동 |
| `fix/scan-skipped-visible` | 스캔 미실행이 "취약점 없음"으로 보이던 문제 | P1 · 리뷰로 이동 |
| `feat/s3-deploy-sse` | S3 배포 진행 SSE 스트리밍 | P1 · 리뷰로 이동 |
| `feat/opa-policy-bundle` | OPA 배포 게이트 정책 번들 | P2 · 리뷰로 이동 |

`integration/night-batch` — 위 5개를 develop에 전부 머지한 합본. **충돌 없음**, 합본 상태로도 전체 검증 통과.

## 검증

| 항목 | develop | 합본 |
|---|---|---|
| core pytest | 1323 | **1339 passed** (+16), 1 skipped |
| webview | 128 | **141 passed** (+13) |
| tsc | OK | OK |
| 골든패스 E2E (moto) | 통과 | **통과** |

골든패스는 5개 브랜치 각각에서도 개별 통과 확인했다.

## 한 줄 요약

- **Workbench** — 같은 HTML을 쓰는 화면이 둘인데 핸들러는 패널에만 37종, 사이드바엔 9종뿐이었고 패널은 고아였다. 핸들러를 `workbenchHost.ts` 공통 호스트로 모으고 두 화면이 상속한다. GitHub·배포·Discord 탭 버튼이 살아난다. 회귀 테스트가 "HTML이 보내는 전 메시지를 호스트가 받는지"를 대조한다.
- **Placeholder** — analyze/deploy는 503 + 원인·다음 행동으로. ops는 폴백을 유지하되(장애 중 도구가 막히는 게 더 나쁘다) "축소 모드"임을 한국어로 밝힌다.
- **스캔 표시** — 코어는 이미 `status:error`를 내려주고 있었는데 화면이 안 읽어서 초록 "취약점 없음 ✓"이 떴다. `scanVerdict()` 순수 함수로 3분기(not_run/vulnerable/clean).
- **S3 SSE** — `/api/deploy/s3/stream` 신설. 기존 라우트는 그대로 둬서 되돌아갈 길을 남겼다. 업로드는 파일마다 보고, 실패는 이벤트로(스트림은 이미 200이라 상태코드로 못 알림). 진행률 막대는 업로드 단계에서만 — 가짜 비율을 만들지 않는다.
- **OPA** — 코어가 질의하는 경로에 올릴 번들이 저장소에 없었다. 규칙 5개 + 테스트 12건 + README.

## 확인 필요한 것 하나

**OPA rego 실행 검증을 못 했다.** 컨테이너에서 `opa` 바이너리를 받을 수 없었다(프록시 차단). 구조·계약 검사는 통과했지만 문법/동작은 미검증이다. 머지 전후로 한 번 돌릴 것:

```bash
opa test policies/ -v
```

## 안 건드린 것

- LLM 라우터 통합 (깨어 있을 때 하기로)
- 고아 코드 5~6천 줄 (세빈님과 결정 필요)
- Dependabot 잔여 확인, 실AWS 검증, 롤백 리허설, Budgets (계정 필요)
- 대화→AI-DLC 핸드오프, 롤백 승인 UI 확인, 마켓 게시 (세빈님 담당)
- `.env` 실파일 (저장소 밖 — Haiku 3로 박혀 있으면 직접 바꿔야 모델 교체가 적용된다)
