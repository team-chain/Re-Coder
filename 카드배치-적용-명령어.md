# 잔여 카드 4건 — 적용 명령어

전부 이 컨테이너에서 검증 완료: 코어 1406 통과 · 웹뷰 152 통과 · tsc OK ·
골든패스 통과 · OPA 12/12 통과 (develop + PR#34 + PR#45 합친 상태 기준).

## 0. 먼저 — 열린 PR 두 개 머지 (검증 끝났음)

- **PR #45 (OPA 정책 번들)** — `opa test policies/ -v` 실제 실행해 12/12 통과 확인함. 바로 머지.
- **PR #34 (E2E 검증)** — 최신 develop(a0c9f96)과 충돌 0, 신규 테스트 65개 통과 확인함. 바로 머지.

PowerShell 은 `&&` 를 지원하지 않는다 — 아래를 **한 줄씩** 실행:

```powershell
gh pr merge 45 --squash --delete-branch
gh pr merge 34 --squash --delete-branch
git checkout develop
git pull
```

## 1. 번들 가져오기

번들은 두 곳(C:\ReCoder, OneDrive 바탕 화면\Recoder)에 있다. 리포 폴더에서:

```powershell
git fetch cards-batch.bundle "refs/heads/*:refs/bundles/*"
```

(다른 폴더의 리포라면 번들 경로를 절대경로로 지정하면 된다.)

## 2. 카드별 브랜치 생성 → push → PR (체리픽 방식이라 히스토리 깨끗)

전부 한 줄씩 실행 (PowerShell 호환):

```powershell
# 카드: Trivy 가 빌드 전 이미지를 스캔 시도 (P1)
git checkout -b fix/trivy-prebuild-gate develop
git cherry-pick cd95e4b
git push -u origin fix/trivy-prebuild-gate
gh pr create --base develop --title "fix(security): 빌드 전 이미지를 Trivy 에 넘기지 않는다" --body "미빌드 이미지는 unverified 로 남기고 승인 강도를 이중 확인으로 올린다. 스캔 error 도 조용히 무시하지 않는다. 실행 시점(빌드 후) 1회 스캔을 보장하고 CRITICAL 이면 기존 컨테이너를 건드리기 전에 차단. 테스트 6종 포함."

# 카드: 스택 감지 오분류 (P2)
git checkout -b fix/stack-detect-fallback develop
git cherry-pick 272e6dc
git push -u origin fix/stack-detect-fallback
gh pr create --base develop --title "fix(infra): 순수 파이썬을 python-fastapi 로 오분류하던 기본값 제거" --body "라우트 감지의 'Default for Python' 과 InfraAgent 의 fastapi 폴백을 제거. 프레임워크 미확정이면 다음 행동이 담긴 422 로 안내. requirements 선언을 소스 검색보다 먼저 보도록 순서 교정. 분류↔CMD 일치를 템플릿별 테스트로 고정 (10종)."

# 카드: 로컬 배포 컨테이너 이름 400 (P1, 리뷰 열)
git checkout -b fix/container-name-tag develop
git cherry-pick f86f215
git push -u origin fix/container-name-tag
gh pr create --base develop --title "fix(workbench): 로컬 배포 컨테이너 이름에서 이미지 태그를 뗀다" --body "recoder-app:v1 입력 시 이름 검증 400 으로 죽던 문제. 이름은 태그·경로·다이제스트를 뗀 조각으로 파생, 이미지에는 태그를 남겨 v1→v2 롤백 유지. 테스트 6종 포함."

# 카드: 확장 UI 요청 계약 고정 (P1)
git checkout -b test/ui-request-contract develop
git cherry-pick f86f215 57c79b6   # 계약 테스트가 컨테이너 이름 파생을 검증하므로 둘 다
git push -u origin test/ui-request-contract
gh pr create --base develop --title "test(contract): 확장이 만드는 배포 요청을 Core 계약에 고정" --body "ApiClient 의 /api/deploy/* 경로 존재, 플랜·실행·S3 본문 필드 일치, 화면 payload 소비·포트 숫자 변환을 소스 수준에서 고정. ※ 이 테스트로 발견: /api/deploy/ec2(+/status,/ready)는 Core 에 라우트가 없어 워크벤치 EC2 배포가 404 를 부른다 — KNOWN_GAPS 로 양방향 고정해 둠."
```

컨테이너 이름 PR 을 먼저 머지하면, 계약 PR 은 `git cherry-pick 57c79b6` 만 하면 된다.

## 3. 새로 발견된 이슈 (보드에 올릴 것)

**워크벤치 EC2 배포는 죽은 엔드포인트를 부른다** — ApiClient 가
`/api/deploy/ec2`, `/api/deploy/ec2/status`, `/api/deploy/ec2/ready` 를
호출하지만 Core 에 해당 라우트가 없다(전수 검색으로 확인). 선택지:
(a) Core 에 EC2 SSH 배포 라우트 구현, (b) 워크벤치에서 EC2 서브탭 숨김.
발표까지 시간이 없으면 (b)가 안전하다.

## 검증 근거

| 항목 | 결과 |
|---|---|
| 코어 pytest (develop+#34+#45+카드4) | 1406 passed, 1 skipped |
| 웹뷰 node --test | 152 pass, 0 fail |
| tsc --noEmit | 통과 |
| golden_path_smoke.py | 통과 |
| opa test policies/ -v | 12/12 PASS |
| 카드 브랜치 상호 충돌 | 없음 (integration/cards-batch 에서 합쳐 검증) |
